<!-- <%- include('../header.ejs') %>

<div class="container">
    <div class="row">

        <h2>TRANG SẢN PHẨM</h2>
        <% cat.forEach(item => { %>
        <div class="col-sm-3 col-md-6 col-lg-4" style="margin-top: 10px;">
            <div class="card" style="width: 300px;">
                <img class="card-img-bottom" src="<%= item.hinhanh %>" alt="Card image">
                <div class="card-body">
                    <h4 class="card-title"><%= item.tensp %></h4>
                    <p class="card-text"><%= item.gia %></p>
                    <a href="#" class="btn btn-primary">Đặt mua</a>
                </div>
            </div>
            <a href="/add-categories" class="btn btn-primary">Thêm mới</a>

        </div>
        
        <% }) %>
    </div>
</div>

<%- include('../footer.ejs') %> -->
